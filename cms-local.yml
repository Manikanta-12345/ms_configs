spring:
  datasource:
    username: root
    password: root@123
    url: jdbc:mysql://host.docker.internal:3306/cms
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  config:
    import: optional:configserver:localhost:8071
current:
  environment: local
